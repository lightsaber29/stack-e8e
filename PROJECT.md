# Career Memory

## 1. Product Goal

과거의 경력, 프로젝트, 문제 해결 경험을 구조화하여 저장하고
자연어 질문을 통해 관련 경험을 찾아주는 개인 Career Memory 시스템을 만든다.

예:

사용자 검색:

"문제를 해결한 경험"

결과:

1. 고객정보 저장 구조 개선
2. Ajax 오류 원인 분석 및 결함 개선
3. 중복 접속 문제 해결

---

## 2. 핵심 사용자 문제

이력서나 자기소개서를 작성할 때 과거의 경험을 정확하게 기억하기 어렵다.

특히 다음 질문에 답할 경험을 찾는 데 시간이 걸린다.

- 문제를 해결한 경험
- 협업한 경험
- 성능을 개선한 경험
- 주도적으로 개선한 경험
- 실패한 경험
- 기술적으로 어려운 문제를 해결한 경험
- React를 활용한 경험
- 금융 프로젝트 경험

따라서 Career Memory는 사용자의 경험을 장기적으로 저장하고
의미 기반 검색을 통해 관련 경험을 회수한다.

---

# 3. MVP Scope

MVP에서는 다음 기능만 구현한다.

## Experience

- 경험 등록
- 경험 수정
- 경험 삭제
- 경험 목록 조회
- 경험 상세 조회

## 대화형 경험 입력 (Conversational Input)

경험을 폼에 직접 입력하는 것 외에, 대화를 통해 정리해서 입력할 수 있다.

- 사용자가 자연어로 경험을 이야기하면 Situation / Problem / Action / Result 등
  구조화된 필드로 정리해준다.
- 빠진 정보는 되물어본다.
- **내용을 창작하거나 윤색하지 않는다.** 사용자가 말한 내용을 구조화할 뿐이다
  (자기소개서 자동 작성은 여전히 Non Goal이다).
- 정리된 결과는 사용자가 최종 확인·수정한 뒤에 저장한다.
- 저장 이후의 경로(임베딩 생성·검색)는 기존과 동일하다 — 로컬 BGE-M3 + pgvector.

이 기능은 사용자 본인의 로컬 Claude Code 세션을 사용한다. 자세한 조건은
ARCHITECTURE.md 2번을 따른다.

### 파일 업로드 (md/pdf)

타이핑 대신 md/pdf 경력 문서를 업로드해서 정리를 시작할 수도 있다.

- 파일 하나에 서로 다른 경험이 여러 개 있으면 자동으로 분리해서 각각 별도의
  정리 결과(draft)로 보여준다. 하나만 있으면 하나만 보여준다.
- 대화형 입력과 동일하게 창작·윤색 없이 문서에 적힌 내용만 정리한다. 문서에 없는
  항목은 비워 둔다 — 대화처럼 되묻지는 않는다(단발 추출).
- 여러 개로 나뉜 경우 각 항목을 개별적으로 확인·수정·저장한다.

## Semantic Search

자연어로 경험을 검색할 수 있어야 한다.

예:

"문제를 해결한 경험"

검색 결과:

- Top 5 경험
- similarity score
- 경험 제목
- Problem
- Action
- Result

## Embedding

경험 저장/수정 시 임베딩을 생성한다.

임베딩은 반드시 로컬 모델을 사용한다.

Default Model:

BAAI/bge-m3

## Database

PostgreSQL + pgvector

---

# 4. Non Goals

MVP에서 구현하지 않는다.

- 자기소개서 자동 작성
- 이력서 자동 생성
- OpenAI API
- 외부 Embedding API
- Local LLM
- 채용공고 자동 분석
- PDF 이력서 생성
- 자체 사용자 계정 시스템 (회원가입/비밀번호 관리)

이 기능들은 이후 단계에서 구현한다.

> **Claude API는 더 이상 Non Goal이 아니다** (2026-08-14 변경).
> 단, "대화형 경험 입력"에 한해서만, 그리고 사용자 본인의 로컬 Claude Code 세션을
> 통해서만 허용한다. 임베딩·검색은 여전히 외부 API를 쓰지 않는다.
> 배경과 대가는 DECISIONS.md 참고.
>
> 자체 계정 시스템은 여전히 Non Goal이다 — 로그인은 사용자가 이미 가지고 있는
> Claude Code 세션을 그대로 쓰며, 앱은 계정도 API 키도 저장하지 않는다.

---

# 5. MVP 화면

## /experiences

경험 목록

## /experiences/new

경험 등록

## /experiences/:id

경험 상세/수정

## /search

경험 검색

검색창:

"문제를 해결한 경험"

결과:

TOP 5 관련 경험
